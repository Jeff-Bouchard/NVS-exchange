diff --git a/web/edit.php b/web/edit.php
index 0b24c70..1fe2751 100644
--- a/web/edit.php
+++ b/web/edit.php
@@ -315,19 +315,19 @@ if (!empty($_POST['value']) && !empty($_POST['days'])) {
 
         <form method="POST">
             <div class="form-group">
-                <input type="number" class="form-input" id="days" name="days" value="<?= $days ?>" min="100" placeholder=" " required>
+                <input type="number" class="form-input" id="days" name="days" value="<?= (int) $days ?>" min="100" placeholder=" " required>
                 <label for="days" class="form-label">Days</label>
                 <div class="form-text">Days (amount of days to store your NVS Record)</div>
             </div>
 
             <div class="form-group">
-                <input type="text" class="form-input" id="address" name="address" value="<?= $address ?>" placeholder=" ">
+                <input type="text" class="form-input" id="address" name="address" value="<?= htmlspecialchars($address, ENT_QUOTES, 'UTF-8') ?>" placeholder=" ">
                 <label for="address" class="form-label">Address (optional)</label>
                 <div class="form-text">Enter your emercoin address (if you want to export this NVS to outer wallet)</div>
             </div>
 
             <div class="form-group">
-                <textarea name="value" id="value" class="form-input" cols="25" rows="6" placeholder=" " required><?= $value ?></textarea>
+                <textarea name="value" id="value" class="form-input" cols="25" rows="6" placeholder=" " required><?= htmlspecialchars($value, ENT_QUOTES, 'UTF-8') ?></textarea>
                 <label for="value" class="form-label">Value (NS=seed1.emercoin.com,seed2.emercoin.com)</label>
                 <div class="form-text">Enter Your Emercoin NVS Value History</div>
             </div>
diff --git a/web/exchange-form-slot.php b/web/exchange-form-slot.php
index 01e62c5..08cc967 100644
--- a/web/exchange-form-slot.php
+++ b/web/exchange-form-slot.php
@@ -54,7 +54,7 @@ if ('generated' === $status) {
     <meta http-equiv="X-UA-Compatible" content="IE=Edge">
     <meta name="viewport" content="width=device-width, initial-scale=1">
     <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js" crossorigin="anonymous"></script>
-    <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery.qrcode/1.0/jquery.qrcode.min.js"></script>
+    <script src="/js/ness-qrcode.js"></script>
     <title>Slot # <?= $slot['id'] ?></title>
     <meta name="description" content="Secure, decentralized Name-Value Storage solution on Emercoin. Protect and manage your data with advanced blockchain technology. Easy, private, and reliable.">
     
@@ -284,6 +284,10 @@ h2, h3 {
     word-break: break-all;
 }
 
+.qr-code-container { display:flex; flex-direction:column; align-items:center; gap:4px; }
+.payment-qr { width:132px; height:132px; padding:8px; background:#fff; border-radius:8px; image-rendering:pixelated; }
+.qr-code-label { font-size:12px; color:var(--text-color); }
+
 .copy-btn {
     background-color: transparent;
     border: 2px solid var(--primary-color);
@@ -431,24 +435,14 @@ h2, h3 {
                         Copy
                     </button>
                     <div class="qr-code-container">
-                        <canvas id="qr-code-<?= $name ?>" width="100" height="100"></canvas>
+                        <canvas class="payment-qr" width="132" height="132" data-payment-address="<?= htmlspecialchars($addr['addr'], ENT_QUOTES, 'UTF-8') ?>" aria-label="QR code for <?= htmlspecialchars($name, ENT_QUOTES, 'UTF-8') ?> payment address"></canvas>
                         <span class="qr-code-label"><?= $name ?></span>
                     </div>
                 </div>
             </h2>
 
-            <script>
-                $(document).ready(function() {
-                    $('#qr-code-<?= $name ?>').qrcode({
-                        text: "<?= htmlentities($addr['addr']) ?>",
-                        width: 100,
-                        height: 100
-                    });
-                });
-            </script>
         <?php endforeach; ?>
     <?php endif; ?>
-<?php endif; ?>
             
             <!-- Display the slot's address and payment address -->
             <h3>Address: 
@@ -706,6 +700,10 @@ h2, h3 {
     <script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js" crossorigin="anonymous">      
     </script>
     <script>
+        document.querySelectorAll('canvas[data-payment-address]').forEach(function(canvas) {
+            NessQRCode.render(canvas, canvas.dataset.paymentAddress);
+        });
+
         function copy(text, target) {
             setTimeout(function() {
                 $('#copied_tip').remove();
diff --git a/web/exchange-form.php b/web/exchange-form.php
index e7ffc32..2974a7c 100644
--- a/web/exchange-form.php
+++ b/web/exchange-form.php
@@ -342,6 +342,7 @@ if ($active && !empty($_POST['address']) && !empty($_POST['pay_address'])) {
  name="address" 
                     class="form-input" 
                     placeholder=" " 
+                    value="<?= htmlspecialchars($address, ENT_QUOTES, 'UTF-8') ?>"
                     required
                 >
                 <label for="address" class="form-label">Your Token Address</label>
@@ -355,6 +356,7 @@ if ($active && !empty($_POST['address']) && !empty($_POST['pay_address'])) {
                     name="pay_address" 
                     class="form-input" 
                     placeholder=" " 
+                    value="<?= htmlspecialchars($pay_address, ENT_QUOTES, 'UTF-8') ?>"
                     required
                 >
                 <label for="pay_address" class="form-label">Your Pay_Address Payment Address</label>
@@ -372,14 +374,15 @@ if ($active && !empty($_POST['address']) && !empty($_POST['pay_address'])) {
                     You can't pay it here 
                     <a href="/slot.php?slot=<?= htmlspecialchars($slot_id) ?>"><?= htmlspecialchars($slot['name']) ?></a>
                 </div>
-            <?php else: ?> 
-                <div class="alert <?php if (!$error) echo 'server-failure'; ?>" role="alert"> 
-                    Privateness V1 - V2 exchange found, but it is inactive (possible server failure)!
-                </div>
             <?php endif; ?>
-    
+
             <button type="submit" class="btn-primary">Create Payment Slot</button>
         </form>
+        <?php else: ?>
+            <div class="alert server-failure" role="alert">
+                Privateness V1 - V2 exchange is inactive.
+            </div>
+        <?php endif; ?>
     </div>
     <script>
         function createMatrixRain() {
@@ -398,4 +401,4 @@ if ($active && !empty($_POST['address']) && !empty($_POST['pay_address'])) {
         createMatrixRain();
     </script>
 </body>
-</html>
\ No newline at end of file
+</html>
diff --git a/web/index.php b/web/index.php
index 1ddda41..48077b1 100644
--- a/web/index.php
+++ b/web/index.php
@@ -42,8 +42,8 @@ if (!empty($_POST['name']) && !empty($_POST['value']) && !empty($_POST['days']))
         }
     }
 } else {
-    $name = '';
-    $value = '';
+    $name = 'dns:your-name.example';
+    $value = 'A=127.0.0.1';
     $address = '';
     $days = 100;
 }
@@ -346,7 +346,7 @@ if (!empty($_POST['name']) && !empty($_POST['value']) && !empty($_POST['days']))
                     name="name" 
                     class="form-input" 
                     placeholder=" " 
-                    value="<?= $name ?>" 
+                    value="<?= htmlspecialchars($name, ENT_QUOTES, 'UTF-8') ?>" 
                     required
                 >
                 <label for="name" class="form-label">Name</label>
@@ -375,7 +375,7 @@ if (!empty($_POST['name']) && !empty($_POST['value']) && !empty($_POST['days']))
                     name="address" 
                     class="form-input" 
                     placeholder=" " 
-                    value="<?= $address ?>"
+                    value="<?= htmlspecialchars($address, ENT_QUOTES, 'UTF-8') ?>"
                 >
                 <label for="address" class="form-label">Address (optional)</label>
                 <div class="form-text">Enter your emercoin address (if you want to export this NVS to outer wallet)</div>
@@ -388,7 +388,7 @@ if (!empty($_POST['name']) && !empty($_POST['value']) && !empty($_POST['days']))
                     class="form-input" 
                     placeholder=" " 
                     required
-                ><?= $value ?></textarea>
+                ><?= htmlspecialchars($value, ENT_QUOTES, 'UTF-8') ?></textarea>
                 <label for="value" class="form-label textarea-placeholder">Value (NS=seed1.emercoin.com,seed2.emercoin.com)</label>
                 <div class="form-text">Enter desired or requested NVS Value</div>
                 
diff --git a/web/slot.php b/web/slot.php
index 9bfce35..4709297 100644
--- a/web/slot.php
+++ b/web/slot.php
@@ -81,6 +81,7 @@ $actual_link = (empty($_SERVER['HTTPS']) ? 'http' : 'https') . "://$_SERVER[HTTP
     <meta charset="utf-8">
     <meta http-equiv="X-UA-Compatible" content="IE=Edge">
     <meta name="viewport" content="width=device-width, initial-scale=1">
+    <script src="/js/ness-qrcode.js"></script>
 
     <style>
         /* Modern Reset and Base Styles */
@@ -391,6 +392,9 @@ $actual_link = (empty($_SERVER['HTTPS']) ? 'http' : 'https') . "://$_SERVER[HTTP
             font-size: 10pt;
         }
 
+.payment-address-row { display:flex; align-items:center; gap:12px; flex-wrap:wrap; }
+.payment-qr { width:132px; height:132px; padding:8px; background:#fff; border-radius:8px; image-rendering:pixelated; }
+
 
 .back-link {
     font-size: 14px;
@@ -516,15 +520,16 @@ $actual_link = (empty($_SERVER['HTTPS']) ? 'http' : 'https') . "://$_SERVER[HTTP
                             SEND
                             <?= $addr['min_sum'] ?> 
                             <?= $name ?> TO: 
-                            <div class="d-flex justify-content-between align-items-center mb-3">
+                            <div class="payment-address-row d-flex justify-content-between align-items-center mb-3">
 
                                 <span class="text-line me-2">
-                                    <?= $addr['addr'] ?> 
+                                    <?= htmlentities($addr['addr']) ?> 
                                 </span>
 
                                 <button onclick="copy('<?= $addr['addr'] ?>','#copy_button_<?= $name ?>')" id="copy_button_<?= $name ?>" class="btn btn-success copy-button">
                                     Copy
                                 </button>
+                                <canvas class="payment-qr" width="132" height="132" data-payment-address="<?= htmlspecialchars($addr['addr'], ENT_QUOTES, 'UTF-8') ?>" aria-label="QR code for <?= htmlspecialchars($name, ENT_QUOTES, 'UTF-8') ?> payment address"></canvas>
                             </div>
                         </h2>
                         
@@ -621,6 +626,10 @@ $actual_link = (empty($_SERVER['HTTPS']) ? 'http' : 'https') . "://$_SERVER[HTTP
     </div>
 
     <script type="text/javascript">
+        document.querySelectorAll('canvas[data-payment-address]').forEach(function(canvas) {
+            NessQRCode.render(canvas, canvas.dataset.paymentAddress);
+        });
+
         function copy(text, target) {
             setTimeout(function() {
                 $('#copied_tip').remove();
@@ -677,4 +686,4 @@ $actual_link = (empty($_SERVER['HTTPS']) ? 'http' : 'https') . "://$_SERVER[HTTP
     <?php endif; ?>
     
 </body>
-</html>
\ No newline at end of file
+</html>
